/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // put any options you had before here
};
module.exports = {
  images: { unoptimized: true },
};
module.exports = nextConfig;